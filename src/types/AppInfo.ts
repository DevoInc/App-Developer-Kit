/**
 * App that calls the query info.
 *
 * @public
 */
export type AppInfo = {
  /**
   * App id
   */
  readonly application: string;
  /**
   * App component
   */
  readonly component: string;
};
